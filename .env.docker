# .env.docker
# Copy to .env before running docker-compose
# Template for Docker deployment environment variables

# ===== Database =====
POSTGRES_USER=secondbrain
POSTGRES_PASSWORD=changeme_in_production
POSTGRES_DB=second_brain

# ===== Docker User Permissions =====
# Ensures files created by container match your user
# Auto-detect: Run `id -u` and `id -g` to get your user/group IDs
USER_ID=1000  # Replace with output of: id -u
GROUP_ID=1000  # Replace with output of: id -g

# ===== Obsidian Vault =====
# IMPORTANT: Use absolute path. Quote if path contains spaces.
# Example: "/Users/yourname/Documents/MyVault"
OBSIDIAN_VAULT_PATH=/path/to/your/obsidian/vault

# ===== LLM API Keys =====
# Required
ANTHROPIC_API_KEY=sk-ant-your-api-key-here

# Optional (for multi-vendor councils)
OPENAI_API_KEY=
GOOGLE_API_KEY=

# ===== Calendar Sync (Optional) =====
# ICS URLs for calendar integration
CALENDAR_WORK_URL=
CALENDAR_PRIVATE_URL=

# ===== Encryption =====
# Auto-generated if not set. Keep secret!
# Generate with: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
API_KEY_ENCRYPTION_KEY=

# ===== Advanced Settings (Optional) =====
# Uncomment to customize

# CHROMA_HOST=chromadb
# CHROMA_PORT=8000
# LOG_LEVEL=INFO
