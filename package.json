{
  "name": "second-brain-app",
  "version": "0.1.0",
  "private": true,
  "description": "AI Second Brain - Local-first knowledge management system",
  "scripts": {
    "dev": "pnpm -r --parallel dev",
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint",
    "e2e": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.57.0",
    "typescript": "^5.3.0"
  },
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=8.0.0"
  }
}
